services:
  bot:
    container_name: github-issues-tracker-bot
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      # Состояние (state.json) на хосте рядом с .env — переживает смену образа без named volume.
      - ./data:/data

